# 换行符统一：仓库内一律 LF，检出时不做平台转换。
# 三人小组跨 Windows / macOS 协作，没有这条时 core.autocrlf 会让同一个文件在不同
# 机器上落成 CRLF / LF 两种形态，git 每次都报 "LF will be replaced by CRLF"，
# 整文件级别的换行差异会淹没真正的改动。
* text=auto eol=lf

# 课程文档与简历样本是二进制格式，禁止任何换行符规范化
*.doc  binary
*.docx binary
*.pdf  binary